# URL of the Deuna realtime backend. In local dev point to the Fastify
# server you're running in parallel (`npm run dev` inside `deuna-api/`).
# In Vercel set this to your Fly.io URL (https://deuna-api.fly.dev).
NEXT_PUBLIC_API_URL=http://localhost:4000
